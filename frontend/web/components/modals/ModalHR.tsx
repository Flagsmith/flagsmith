import React, { FC } from 'react' // we need this to make JSX compile

type ModalHRType = {}

const ModalHR: FC<ModalHRType> = ({}) => {
  return <hr className='my-0 py-0' />
}

export default ModalHR
