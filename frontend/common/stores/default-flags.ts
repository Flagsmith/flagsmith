import integration_data from './integration-data.json'
import segment_operators from './segment-operators.json'

const defaultFlags = {
  integration_data,
  segment_operators,
}

export { defaultFlags }
