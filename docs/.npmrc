# Release-age cooldown for npm. See frontend/.npmrc for why this lives here
# rather than in renovate.json. No exclusions: docs depends on no internal
# packages. Needs npm >= 11.10.0; older npm ignores this and installs anyway.
min-release-age=7
